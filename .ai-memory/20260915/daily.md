# 2026-09-15 · 会话记录

## [01:0x] 动作: 顶栏响应式 + 示例项目模板（提交 1f2e1fb，已推送）

- 需求：① 页面缩小时顶栏不美观（按钮文字被压成竖排）；② 用现有组件做几份经典项目实例并在项目管理里展示
- 顶栏：`.topbar` flex-wrap:nowrap + overflow-x:auto（隐藏滚动条）；`.btn/.chip/.run-btn { white-space: nowrap; flex-shrink: 0 }`；双文本按钮 + 三级媒体查询（1460/1240/1024px）；按钮补 aria-label（窄屏下 E2E 定位稳定）
- 模板：`apps/web/src/projectTemplates.ts` 4 个（温湿度显示 / 超声波测距 / 电机调速 / 串口调试），覆盖单总线、I2C、5V 分压、器件直连、TX-RX 交叉、UART0 占用等教学点
- store：`loadTemplate`（深拷贝载入，防污染模板；可撤销）；`loadSampleProject` 改为委托默认模板（去掉 80 行硬编码示例数据）
- UI：`ProjectPanel` 顶部模板区（`.template-card` 网格：名称/说明/要点标签）
- 测试：`templates.test.ts` 6 例（**强制每个模板无 error**、id 不重复、深拷贝、未知 id 提示、默认示例等同、可撤销）；冒烟 +4 项（模板 4 个、载入后 4 节点、电机模板运行无 error、1100px 顶栏 58px 不换行）
- 顺带：示例实例 id `c-*` → `t1-*`，用 node 脚本批量修正 7 个文件引用
- 验证：109 例全绿（51/15/43）、冒烟 37 项 0 控制台错误、typecheck 0 error、构建成功
- 坑：PowerShell 下 git commit message 含全角引号/破折号会被截断（pathspec 错误）→ 改简洁消息后成功
- 阻塞：无

## [03:5x] 动作: 浅色主题改暖米白 + 去品牌副标题（提交 d8564a1，已推送）

- 用户偏好：浅色要**暖米白**（护眼，不要纯白/冷灰蓝）、品牌区只留标题
- 中性色系整体调暖（bg/panel/panel-soft/border/text/muted/overlay/canvas-dot/chip/diag），text 用暖黑 `#2a2621`；主色与语义色保留
- 移除副标题小字 + 样式与窄屏规则
- 验证：check-contrast light **0 项**、冒烟 45 项、typecheck 0 error
- **推送已恢复**：连同积压的 9d70842 / bdc8976 一起推送成功，本地与远端同步

## [03:3x] 动作: 面包板版式改版 + 组件节点文案修复（提交 9d70842，**待推送**）

- 用户截图反馈两问题，均为真 bug：
  1. **面包板连线被遮住**：列 Handle 定位在列正上方（`top:-4px`）→ 正好落在「+ 电源轨」区域且板体不透明 → 线与 Handle 被盖住。改版（采纳用户建议）：电源轨移到**左右两侧**（Handle 在节点左右边缘中点）、列接线点落到**下边缘**并与孔位列共用同一套 grid 保证对齐（实测偏差 **0.0px**）、移除底部说明行
  2. **电阻节点显示 `resistor` 与"未接上拉"**：① 型号行 `displayName.split(' ').slice(1)` 对「电阻（1/4W）」这种无空格名称切成空串 → 退化成 slug；② 徽标无条件渲染"未接上拉"，没判断组件是否真有该选项。修复：型号前缀正则剥离；徽标按 `def.portOptions` 判定，并为 `resistance` 显示阻值（220Ω）
- 冒烟 **45 项**（+2 断言：列对齐、电阻节点文案）、前端单测 49 例、typecheck 0 error
- 推送状态：本地领先 1 个提交（9d70842），GitHub 连接被重置，网络恢复后 `git push origin master` 即可

## [03:0x] 动作: 面包板 + R-23 + 结果面板裁切修复（提交 a86a5ea + 后续，已推送）

- **修 UI 裁切**：`.result-list` 缺 `min-height: 0` → 诊断展开后内容溢出面板被切掉（"显示不完全"）→ 修复并加冒烟断言
- **清理任务清单**：确认无定时任务（无 automations 目录），清理会话 todo
- **面包板（G-05/Q-T4 简化模型）**：12 列（passive、非必需）+ 正负电源轨；`renderAs: 'breadboard'` 契约字段驱动孔位网格渲染（BreadboardNode）
  - 实现比文档原方案简单：列即端口 → 同端口多条连线由并查集天然合并，**无需合成 bus 连线**
  - 新规则 **R-23**（同 net 多个输出短接）；修 **R-19 漏报**（`hasPassiveComponent` 改按元件判定：`isPassiveElement`）
  - 新模板「面包板一拖多」（1 GPIO 驱动 3 LED），校验通过
- 验证：123 例（59/15/49）、冒烟 43 项、typecheck 0 error、seed 幂等（15 组件/21 规则）
- 文档：TDD G-05/Q-T4/§4.4/§6.5/§18.5、README 同步

## [02:3x] 动作: 诊断按规则聚合（Q-T5，提交 74418b5，已推送）

- 实测大项目诊断分布：8 条诊断（R-15 ×1 / R-16 ×5 / R-17 ×2），**0 组完全重复** → 冗余不是"同引脚"而是"同规则多处命中"（未接线项目下 R-01 会按端口逐条产出更明显）
- **决策修正**：改为按**规则码**聚合（文档 Q-T5 原写"按引脚"）
- 实现：`components/diagGroups.ts` 纯函数 + ValidationPanel 分组渲染（单处平铺 / 多处折叠 N 处可展开 / 展开态按 code 记忆）+ `.diag-group*` 样式
- 效果：8 行 → 3 个视觉块
- 验证：117 例测试（53/15/49）、冒烟 40 项（+2：折叠时明细 0、展开 R-16 为 5 条）、typecheck 0 error、构建成功
- 文档：TDD Q-T5/M-06/§18.5 同步，README 测试表更新

## [02:0x] 动作: M-04 性能基准 + 综合实验大项目模板（提交 dd22948，已推送）

- 基准脚本 `apps/web/scripts/bench.mjs`（四项：首屏 / 端到端校验 / 画布帧率 / 后端 P95）+ 报告 `docs/性能基准.md`
- 实测（生产构建）：帧率 5.6ms/帧 ≈178 FPS（27 节点/58 连线，拖动中同值，长任务 0）；P95 16.6ms（100/150，200 次）；首屏 542ms（4G 节流）；端到端 63ms → **NFR-01/02 全达标**
- 方法学（3 个坑）：dev server 数据是假象（React 开发模式开销，V8 剖析证实）；逐次 mouse.move 的 CDP 往返污染 longtask；headless rAF 不节流（判读用帧间隔中位数）
- 大项目模板：综合实验项目（26 组件 / 59 连线）—— LED 串联支路 ×6、器件直连、I2C 双设备、独立供电
- 修 R-03 真缺陷：I2C 多设备共享 SDA/SCL 被误报 → 放行全 I2C 端口共享（R-13 管地址冲突），+2 例单测
- 渲染优化：节点 memo + data 引用稳定化（未观测到显著差异，保留作保险）
- 验证：111 例（53/15/43）、冒烟 38 项、typecheck 0 error、构建成功
- 文档：TDD §12.1 待验证 → 实测、T-UV1~3 与 M-04 标记完成、§18.5 补性能行；README 加性能基准行与模板说明
