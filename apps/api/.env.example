# 复制为 .env 后按需修改
# 本地开发（SQLite）：相对路径基于 schema.prisma 所在目录 → apps/api/prisma/dev.db
DATABASE_URL="file:./dev.db"

# 生产（PostgreSQL）：需同时把 prisma/schema.prisma 的 provider 改为 postgresql
# DATABASE_URL="postgresql://user:password@localhost:5432/sim_mcu?schema=public"

PORT=3000
ANON_PROJECT_TTL_DAYS=30
